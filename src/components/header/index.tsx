import { MenuLinks } from "./menuLinks";
import { NavBarContainer } from "./navBarContainer";

export const Header = () => {
  return (
    <NavBarContainer>
      <MenuLinks />
    </NavBarContainer>
  );
};
